# Automatically populated with IP and Port during installation
PANEL_IP = 'auto-detected-ip'  # Automatically detected IP address (VPS IP)
PANEL_PORT = 'user-provided-port'  # User-specified panel port
